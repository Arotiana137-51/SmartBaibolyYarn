// App version used to decide when to replay the onboarding flow (color
// selection + tutorials). Bump this once per release, same habit as the
// android `versionName` in android/app/build.gradle. Onboarding replays on
// first install and on the first launch after this string changes.
export const APP_VERSION = '1.2.4';
